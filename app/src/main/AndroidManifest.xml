<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.linkdev.localizatitonsample">

    <application
        android:name="com.linkdev.localizatitonsample.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.linkdev.localizatitonsample.ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities_sample.NewsActivity" />
        <activity android:name=".ui.activities_sample.NewsDetailsActivity" />
        <activity android:name=".ui.fragments_sample.FragmentsSampleActivity" />
        <activity android:name=".ui.navigation.NavigationActivity" />
        <activity android:name=".ui.one_activity.MenuActivity" />
    </application>

</manifest>